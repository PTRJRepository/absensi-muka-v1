# ABSENSI MUKA — Full Refactor Planning

## Project
`D:/Gawean Rebinmas/Absensi_Muka`

## Status Sekarang

### ✅ Working Endpoints (confirmed by curl):
- /api/employees, /api/divisions, /api/machines, /api/attendance/summary
- /api/dashboard/stats, /api/reports/daily, /api/import/batch/1/status, /api/import/formats
- /api/quality/summary, /api/realtime/stats, /api/monitoring/sync-status/1
- All quality, monitoring, scheduler, sync endpoints
- All SSE realtime endpoints

### ⚠️ Issues to Fix:
1. `/api/attendance/daily` — empty (needs date range params)
2. `/api/attendance/employee/:code` — empty (no data for test code)
3. `/api/import/batch/1/status` — only batch 1, others may fail
4. Server crashes on scheduler sync startup (unhandled ZKError rejection)
5. 29,028 unmapped employees in DB
6. 100 duplicate scan groups, 716 unmapped device users

## Goal: Full Modernization Plan

### Step 1: ARCHITECTURE SURVEY
1. Read ALL route files in src/api/routes/ — list every endpoint
2. Read src/lib/db.ts — how queries are built
3. Read src/server.ts — how routes are registered
4. Read src/api/middleware/ — what middleware exists
5. List all files in src/ — understand full project structure
6. Run `sqlcmd -S 10.0.0.110 -U sa -P "<DB_PASSWORD>" -d rebinmas_absensi_monitoring -Q "SET NOCOUNT ON; SELECT name FROM sys.tables ORDER BY name" -W` to list ALL tables
7. Check attendance_imports has data: count rows

### Step 2: WRITE PLAN.md
Write comprehensive `D:/Gawean Rebinmas/Absensi_Muka/REFACTOR_PLAN.md`:

```markdown
# Refactor Plan

## 1. Critical Fixes (blocks production)
- [list issues that cause crashes/errors]

## 2. Data Layer Issues
- [unmapped data, wrong mappings, missing data]

## 3. API Improvements
- [endpoints that need better data, missing endpoints]

## 4. Code Quality
- [TypeScript issues, code duplication, error handling]

## 5. Modernization
- [suggestions for modern patterns, middleware, validation]

## 6. Frontend Readiness
- [what endpoints need to exist for a complete UI]

## Priority: P0 (Must Fix), P1 (Should Fix), P2 (Nice to Have)
```

### Step 3: Verify Plan
For each issue in plan, note:
- File and line number
- Exact problem
- Exact fix approach
- Estimated complexity (small/medium/large)

### Rules
- NO fixing in this phase — PLAN ONLY
- Use sqlcmd for DB introspection
- Read every route file
- Keep plan actionable and specific
- Max 30 turns — stop if not done and report progress

Output: Write `D:/Gawean Rebinmas/Absensi_Muka/REFACTOR_PLAN.md`
