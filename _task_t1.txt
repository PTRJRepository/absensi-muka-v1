You are a debugging agent. Fix the QualityPage loading stuck issue.

Project: D:/Gawean Rebinmas/Absensi_Muka
Frontend: D:/Gawean Rebinmas/Absensi_Muka/frontend (Vite + React)
Backend running at: http://localhost:8004

PROBLEM:
Page /laporan (Laporan Kualitas) is stuck showing "Menyegarkan..." forever.
Browser console error:
  TypeError: Cannot read properties of undefined (reading 'healthy_count')

BACKGROUND:
- api() function in frontend/src/lib/api.ts strips {success, data} wrapper and returns inner data directly
- The /api/quality/summary endpoint returns: { success: true, data: { period_days, total_scans, mapped_employees, unmapped_codes, failed_batches, completed_batches, status } }
- After api() strips the wrapper, the queryFn receives the raw data object
- The QualityPage queryFn (line 14-39) already does: `const raw = await api<{...}>('/api/quality/summary')` then transforms it to QualityReport shape
- But there are also 2 other queries: /api/quality/unmapped and /api/quality/duplicates

TASK:
1. Read frontend/src/components/features/quality/QualityPage.tsx — FULL content
2. Read frontend/src/lib/api.ts — FULL content
3. Test these endpoints via curl:
   curl http://localhost:8004/api/quality/unmapped
   curl http://localhost:8004/api/quality/duplicates
4. Identify which of the 3 queries (quality summary OR unmapped OR duplicates) is causing the hang
5. Fix ONLY the problematic query — patch the file, do NOT rewrite it entirely
6. Rebuild: cd D:/Gawean\ Rebinmas/Absensi_Muka/frontend && npx vite build
7. Report: what was broken, what you fixed, and the build output (last 5 lines)
