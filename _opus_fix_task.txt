# Opus Exploration Task: Fix Absensi_Muka DB + API Issues

## Goal
Fix the Absensi_Muka web app on port 3004 (Vite frontend → 8004 backend) so matrices and attendance show real data, not "no data".

## Current State

### API Test Results (port 8004):
- `/api/machines` → 200 ✅
- `/api/employees` → 200 ✅
- `/api/divisions` → 200 ✅
- `/api/attendance/daily` → 200 ✅ (returns data wrapped in `{success, data, message}`)
- `/api/attendance/monthly-matrix?year=2026&month=6` → **404 NOT_FOUND** ❌
- `/api/attendance/summary?date=2026-06-20` → 200 ✅
- `/api/attendance/available-months` → **404 NOT_FOUND** ❌
- `/api/attendance/employee/:code` → probably 404
- `/api/dashboard/summary` → **HTTP 500**: `Could not use view or function 'vw_attendance_final' because of binding errors` ❌

### DB State (rebinmas_absensi_monitoring on 10.0.0.110:1433):

**Existing views:**
- vw_attendance_final ⚠️ (binding error when queried)
- vw_attendance_daily_summary ✅
- vw_attendance_monthly_summary ✅
- vw_attendance_zkteco_monthly_summary ✅
- vw_attendance_zkteco_daily_summary ✅
- vw_attendance_zkteco_final ✅
- vw_sync_latest_status ✅

**Missing objects:**
- `vw_attendance_monthly_matrix` — MISSING (route depends on it)
- `vw_attendance_monthly_summary_v2` — MISSING (route depends on it)
- `attendance_manual_corrections` table — MISSING (route depends on it)

**Data tables:**
- `attendance_imports` → RENAMED to `attendance_imports_old`
- `attendance_import_batches` ✅
- `attendance_scan_logs` ✅
- `employees` ✅ (4182 rows)
- `divisions` ✅
- `gangs` ✅ (empty)
- `attendance_machines` ✅ (16 rows)
- `machine_connection_logs` ✅
- `attendance_sync_logs` ✅
- `zkteco_hr_employee_map` ✅

### DB Connection:
```
Server: 10.0.0.110
Port: 1433
User: sa
Password: <DB_PASSWORD>
Database: rebinmas_absensi_monitoring
```

## Problems to Fix

### P0 — No data in matrices/attendance:

1. **`vw_attendance_monthly_matrix` missing** — Monthly matrix view doesn't exist
   - Route: `GET /api/attendance/monthly-matrix`
   - Migration file: `migrations/014_monthly_matrix_view.sql`
   - The migration references `attendance_imports` table but it was renamed to `attendance_imports_old`

2. **`attendance_manual_corrections` missing** — Manual corrections table doesn't exist
   - Used by correction CRUD routes

3. **`vw_attendance_final` binding error** — View references `attendance_imports` but table is `attendance_imports_old`
   - Causes `/api/dashboard/summary` → 500

4. **`attendance_imports` alias needed** — Views reference `attendance_imports` but table is renamed
   - Fix: CREATE OR ALTER VIEW `attendance_imports` AS SELECT * FROM `attendance_imports_old`
   - OR: CREATE SYNONYM attendance_imports FOR attendance_imports_old

### P1 — Routes returning 404:

5. **`/api/attendance/monthly-matrix`** — returns 404 despite route existing in source
   - Investigate: ts-node may be loading old source, or there's a route conflict

6. **`/api/attendance/available-months`** — returns 404

7. **`/api/attendance/employee/:code`** — likely 404

### DB Schema Details:

**attendance_imports_old columns:**
id, employee_id, employee_code, division_code, gang_code, attendance_date, attendance_year, attendance_month, check_in_at, check_out_at, attendance_status, has_work, is_leave, is_sick, is_holiday, overtime_hours, source, source_reference, batch_id, raw_scan_log_id, created_at, needs_manual_review

**attendance_scan_logs columns:**
id, machine_code, device_user_id, employee_code, parsed_employee_code, raw_device_user_id, scan_time, scan_date, pin, sn, verified, in_out_mode, work_code, temp, raw_data, loc_code, created_at, mapping_status, employee_name_raw

**employees columns:**
id, employee_code, employee_name, division_id, gang_id, is_active, created_at, updated_at

**attendance_imports_old** has data but migration views still reference `attendance_imports`

## What You Must Do

1. **Probe the actual DB schema** — Connect with pymssql and run these:
   - `SELECT TOP 1 * FROM vw_attendance_final` — get actual error
   - `SELECT TOP 1 * FROM attendance_imports_old` — verify data
   - `SELECT COUNT(*) FROM attendance_scan_logs` — verify scan data exists
   - `SELECT TOP 5 * FROM attendance_scan_logs WHERE parsed_employee_code IS NOT NULL` — check mapping

2. **Create missing DB objects** (execute via pymssql):
   - `attendance_imports` VIEW as alias for `attendance_imports_old`
   - `attendance_manual_corrections` table
   - `vw_attendance_monthly_matrix` view (fix references to `attendance_imports` → `attendance_imports_old`)
   - `vw_attendance_monthly_summary_v2` view

3. **Fix `vw_attendance_final` binding error**

4. **Investigate route 404** for `/api/attendance/monthly-matrix`:
   - The route exists in dist file but returns 404
   - May be a route ordering issue (more specific routes after generic)
   - May be ts-node hot-reload issue — try restarting server

5. **After fixes, verify each endpoint works**

## Return Format

Return:
1. List of DB queries run + results
2. List of SQL CREATE statements executed
3. Results of API endpoint tests after fixes
4. Any remaining issues
